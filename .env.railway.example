# Railway Environment Variables Template
# Copy these to your Railway project's environment variables

# ============================================
# DATABASE CONFIGURATION (Railway MySQL Plugin)
# ============================================
# Railway will automatically provide these when you add MySQL plugin
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE

# If using Railway MySQL plugin, it provides MYSQL_URL automatically
# Otherwise, set these manually:
MYSQL_URL=mysql://root:password@mysql.railway.internal:3306/railway

# OR set individual variables:
DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_NAME=railway
DB_USER=root
DB_PASSWORD=your_secure_password_here

# ============================================
# WORDPRESS CONFIGURATION
# ============================================
WORDPRESS_DB_HOST=${DB_HOST}:${DB_PORT}
WORDPRESS_DB_NAME=${DB_NAME}
WORDPRESS_DB_USER=${DB_USER}
WORDPRESS_DB_PASSWORD=${DB_PASSWORD}

# WordPress Site URL (Railway provides this as PUBLIC_URL)
WP_HOME=${RAILWAY_PUBLIC_DOMAIN}
WP_SITEURL=${RAILWAY_PUBLIC_DOMAIN}

# ============================================
# WORDPRESS SECURITY KEYS
# ============================================
# Generate unique keys at: https://api.wordpress.org/secret-key/1.1/salt/
# IMPORTANT: Replace these with your own unique keys!

AUTH_KEY='put your unique phrase here'
SECURE_AUTH_KEY='put your unique phrase here'
LOGGED_IN_KEY='put your unique phrase here'
NONCE_KEY='put your unique phrase here'
AUTH_SALT='put your unique phrase here'
SECURE_AUTH_SALT='put your unique phrase here'
LOGGED_IN_SALT='put your unique phrase here'
NONCE_SALT='put your unique phrase here'

# ============================================
# WORDPRESS SETTINGS
# ============================================
WP_DEBUG=false
WP_DEBUG_LOG=false
WP_DEBUG_DISPLAY=false
WP_ENVIRONMENT_TYPE=production

# Security
DISALLOW_FILE_EDIT=true
AUTOMATIC_UPDATER_DISABLED=true
WP_POST_REVISIONS=5
WP_MEMORY_LIMIT=256M

# ============================================
# SMTP EMAIL CONFIGURATION (Optional)
# ============================================
# For sending emails from WordPress
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@itskanal.com
SMTP_FROM_NAME='iTS KANAL Services'

# ============================================
# CDN / ASSETS (Optional)
# ============================================
# If using a CDN for static assets
# CDN_URL=https://cdn.yourdomain.com

# ============================================
# REDIS CACHE (Optional)
# ============================================
# If using Railway Redis plugin for object caching
# REDIS_URL=redis://default:password@redis.railway.internal:6379

# ============================================
# CUSTOM SETTINGS
# ============================================
# Railway automatically provides:
# - PORT (usually 80 for web services)
# - RAILWAY_ENVIRONMENT (production, staging, etc.)
# - RAILWAY_PUBLIC_DOMAIN (your app's public URL)
# - RAILWAY_PROJECT_ID
# - RAILWAY_SERVICE_ID
